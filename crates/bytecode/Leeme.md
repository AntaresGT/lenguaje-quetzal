# bytecode

Define el conjunto de instrucciones (`Instruccion`), la tabla de constantes, el formato `ModuloCompilado` y el generador que convierte el AST validado en bytecode listo para ejecutarse en `maquina_virtual`.
