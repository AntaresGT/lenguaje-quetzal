# lsp

Servidor de lenguaje (Language Server Protocol) del Lenguaje Quetzal: diagnósticos, autocompletado y hover para editores.

**Estado: futuro.** Este crate es un esqueleto declarado; se implementará en una fase posterior reutilizando `lexico`, `sintaxis` y `semantica`.
